Overview


Setup
- Install node js from http://nodejs.org/
- Install nodemon by running this [everything after the dollar sign] in your terminal  ~~~>$ npm install -g nodemon


Install all npm dependencies
This will install socket io, express and every other module we need in node 
- Run this is your terminal ~~> npm install


Start the server
- nodemon server.js
